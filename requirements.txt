# ── Layer 1: Data Collection ─────────────────────────────────────────────────
requests==2.31.0            # HTTP calls to all APIs
biopython==1.83             # Entrez / PubMed E-utilities helper
tenacity==8.2.3             # Retry logic with exponential backoff
ratelimit==2.2.1            # Per-source rate limiting

# ── Layer 2: Web Scraping (full-text fallback) ────────────────────────────────
beautifulsoup4==4.12.3      # HTML parsing for full-text extraction
scrapy==2.11.0              # Structured web crawling
playwright==1.43.0          # JS-rendered journal pages
pdfminer.six==20221105      # PDF text extraction
pymupdf==1.28.0             # Faster PDF parsing (PyMuPDF) — bumped for pymupdf4llm compat
pymupdf4llm==1.28.0         # Layout-aware extraction: multi-column reading order,
                            # table structure preservation, structural header detection
                            #
                            # OPTIONAL SYSTEM DEPENDENCY — Tesseract OCR:
                            # pymupdf4llm can OCR scanned/image-only PDF pages as a
                            # last-resort fallback (see nlp/fulltext/pdf_parser.py).
                            # This requires the `tesseract` binary on PATH — it is
                            # NOT a pip package and will not be installed by this
                            # requirements file. Without it, OCR fallback silently
                            # degrades to "no text found" rather than erroring.
                            #   macOS:  brew install tesseract
                            #   Ubuntu: apt install tesseract-ocr

# ── Layer 3: NLP Processing ───────────────────────────────────────────────────
transformers==4.40.0        # BioBERT / PubMedBERT for NER
torch==2.3.0                # PyTorch backend for transformers
spacy==3.7.4                # Rule-based NLP + pipeline
numpy>=1.24.0               # Array operations for embeddings and ML
scikit-learn==1.4.2         # Article type classifier
sentence-transformers==2.7.0  # Paper embeddings for similarity

# ── Layer 4a: Storage ─────────────────────────────────────────────────────────
neo4j==5.20.0               # Neo4j Python driver
psycopg2-binary==2.9.9      # PostgreSQL driver
sqlalchemy==2.0.30          # ORM for PostgreSQL metadata
elasticsearch==8.13.0       # Full-text search index

# ── Layer 4b: Scheduling ─────────────────────────────────────────────────────
apscheduler==3.10.4         # Job scheduler (daily/weekly runs)
redis==5.0.4                # Optional: Celery task broker

# ── Utilities ────────────────────────────────────────────────────────────────
trafilatura==1.12.2         # Web content extraction (full-text scraping)
psutil==5.9.8               # System/process utilities
python-dotenv==1.0.1        # Load secrets from .env file
loguru==0.7.2               # Structured logging
pydantic==2.7.1             # Data validation / schemas
tqdm==4.66.4                # Progress bars for batch jobs
filelock>=3.12.0            # File-based locking for embedding store

# ── Relevance Filter ──────────────────────────────────────────────────────────
pyyaml==6.0.1               # organisms.yaml config loading

# ── Testing ───────────────────────────────────────────────────────────────────
hypothesis==6.155.0         # Property-based testing framework

# ── API Layer ─────────────────────────────────────────────────────────────────
fastapi==0.109.0            # Modern web framework for building APIs
uvicorn==0.27.0             # ASGI server for FastAPI
